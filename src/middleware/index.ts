/**
 * Public entry point for routewatch middleware adapters.
 * Import the appropriate factory for your framework.
 */
export { createExpressMiddleware, MiddlewareOptions } from './expressMiddleware';
